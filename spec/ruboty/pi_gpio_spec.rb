require 'spec_helper'

describe Ruboty::PiGpio do
  it 'has a version number' do
    expect(Ruboty::PiGpio::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
